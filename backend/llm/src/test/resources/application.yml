spring:
  flyway:
    enabled: true
    # LLM modülü kendi başına test ediliyor; ontoloji şemasına ihtiyacı yok.
    locations:
      - classpath:db/migration/llm
    clean-disabled: false

logging:
  level:
    org.flywaydb: WARN
    com.investor.llm: WARN
