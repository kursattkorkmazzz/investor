spring:
  flyway:
    enabled: true
    locations:
      - classpath:db/migration/ontology
      - classpath:db/migration/marketdata
    clean-disabled: false

investor:
  market-data:
    base-url: http://localhost:0   # testlerde WireMock ile ezilir
    staleness-tolerance: PT5M

logging:
  level:
    org.flywaydb: WARN
